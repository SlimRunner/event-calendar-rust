[package]
name = "event-calendar-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
time = { version = "0.3", features = ["serde", "parsing", "formatting"] }
yaml_serde = "0.10.4"
